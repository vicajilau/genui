name: genui_builder
description: AST code generator for inspecting Generative UI components.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  analyzer: ^13.0.0
  build: ^4.0.6
  source_gen: ^4.2.3
  glob: ^2.1.3
  dart_style: any
  genui_annotations: any

dev_dependencies:
  lints: ^6.0.0
