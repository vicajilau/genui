import 'package:flutter_test/flutter_test.dart';

import 'package:genui_engine/genui_engine.dart';

void main() {
  test('adds one to input values', () {
    /*
    final engine = GenUIEngine(registry: globalGenUIRegistry);
    expect(engine.parse({"type": "Text", "properties": {"text": "Hello World"}}), 3);
    expect(engine.parse({"type": "Text", "properties": {"text": "Hello World"}}), -6);
    */
  });
}
